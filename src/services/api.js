const URL_BASE = "localhost:8888/"
let end_points = {
    users: URL_BASE + "users",
    jobs: `${URL_BASE}jobs`,
    candidates: URL_BASE + "candidates",
    applications: `${URL_BASE}applications`
}

export default end_points